# Demo Script

1. Start backend and frontend:
   docker-compose up --build

2. Start edge inference:
   python edge/infer.py --use_ultralytics

3. Open dashboard:
   http://localhost:3000

4. Show live updates as objects appear/disappear.

5. Trigger reorder alert from UI.
